<?xml version="1.0" encoding="UTF-8" ?>
<server description="This file was generated by the 'collective join' command on 2017-05-29 14:34:06 EDT.">
    <featureManager>
        <feature>collectiveMember-1.0</feature>
		<feature>clusterMember-1.0</feature>
    </featureManager>
	<clusterMember name="FSIMS_server_cluster"/>
	
    <!-- Define the host name for use by the collective.
         If the host name needs to be changed, the server should be
         removed from the collective and re-joined or re-replicated. -->
    <variable name="defaultHostName" value="lrcdqpvsdvap008.cihs.ad.gov.on.ca" />

    <!-- Remote host authentication configuration -->
    <hostAuthInfo rpcUser="vadmin"
                  rpcUserPassword="Shellfish!!" />

    <!-- Connection to the collective controller -->
    <collectiveMember controllerHost="LRCDQPVSDVAP008.cihs.ad.gov.on.ca"
                      controllerPort="9450" >
					<failoverController host="LRCDQPVSDVWE003.cihs.ad.gov.on.ca" port="9450"/>
					<failoverController host="LRCDQPVSDVWE001.cihs.ad.gov.on.ca" port="9450"/>
	</collectiveMember>

    <!-- clientAuthenticationSupported set to enable bidirectional trust -->
    <ssl id="defaultSSLConfig"
         keyStoreRef="defaultKeyStore"
         trustStoreRef="defaultTrustStore"
         clientAuthenticationSupported="true" />

    <!-- inbound (HTTPS) keystore -->
    <keyStore id="defaultKeyStore" password="{xor}MjoyPTotFAwPPiwsKDAtOw=="
              location="${server.config.dir}/resources/security/key.jks" />

    <!-- inbound (HTTPS) truststore -->
    <keyStore id="defaultTrustStore" password="{xor}MjoyPTotFAwPPiwsKDAtOw=="
              location="${server.config.dir}/resources/security/trust.jks" />

    <!-- server identity keystore -->
    <keyStore id="serverIdentity" password="{xor}MjoyPTotFAwPPiwsKDAtOw=="
              location="${server.config.dir}/resources/collective/serverIdentity.jks" />

    <!-- collective truststore -->
    <keyStore id="collectiveTrust" password="{xor}MjoyPTotFAwPPiwsKDAtOw=="
              location="${server.config.dir}/resources/collective/collectiveTrust.jks" />
			  
</server>
